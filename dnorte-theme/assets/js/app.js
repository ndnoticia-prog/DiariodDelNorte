import '../scss/app.scss';

// Punto de entrada de JS del front-end. Vacío por ahora — el anti-parpadeo de modo
// oscuro vive inline en header.php (debe ejecutarse antes del primer paint, así que
// no puede depender de este bundle, que carga después).
